name: face_attendance_app
description: A face recognition based attendance application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^0.13.5
  camera: ^0.10.0
  path_provider: ^2.0.11
  path: ^1.8.2
  intl: ^0.18.0
  provider: ^6.0.5
  shared_preferences: ^2.0.15
  # permission_handler: ^10.2.0  # Temporarily disabled due to compatibility issues
  fluttertoast: ^8.2.1
  image: ^4.0.17
  google_mlkit_face_detection: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/